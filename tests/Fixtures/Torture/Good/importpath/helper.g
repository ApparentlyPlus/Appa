// Companion file for ../import_path.g — imported by quoted relative path, not by
// bare module name. Not picked up as its own torture case: it has no kernel/entry,
// and the harness only globs *.g directly inside good/, not subdirectories.
int func addTwo(int x) { return x + 2; }
